# Ignore Git files
.git/
.gitignore

# Ignore Python cache and virtual environment files
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
env/
venv/
.venv/
pip-log.txt
pip-delete-this-directory.txt
.webdevManifest.json

# Ignore OS-specific files
.DS_Store
Thumbs.db

# Ignore IDE and editor specific files
.vscode/
.idea/
*.swp
*.swo

# Ignore original app.py if it's only for local dev and not part of deployment
# We might uncomment this later if app.py is confirmed to be local-only
# app.py

# Docker specific files (if any were used locally)
Dockerfile
docker-compose.yml

# Render specific files (as they were in the original repo)
render.yaml

# Any local test data or large assets not meant for deployment
# e.g. local_data/
# *.mp4
# *.mov
# *.avi
# *.pkl

# Node modules (if any frontend part was developed with Node.js locally)
node_modules/

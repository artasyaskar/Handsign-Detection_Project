__pycache__/
*.pyc
node_modules/
.cache/
build/
.git/
.gitignore
README.md
venv/
*.md # If there are other markdown files not needed in context
# Add any other files/directories that are not needed for the Docker image build
# For example, local development files or large datasets not used by the app directly at build time.
